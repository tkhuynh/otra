<strong><%= @show.venue %></strong>	
<%= @show.show_date %><br>
<i>Slots to fill: </i><%= @show.slots - @show.performances.count %><br>
Confirmed bands: <%= @show.performances.count %><br>
<% @show.performances.all.each do |perf| %>
	<strong><%= Band.find(perf.band_id).name %></strong>
<% end %>
